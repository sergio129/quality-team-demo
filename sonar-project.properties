# Información del proyecto
sonar.projectKey=quality-team2
sonar.projectName=Quality Team
sonar.projectVersion=1.0.0
sonar.organization=default

# URL de tu servidor SonarQube
sonar.host.url=http://localhost:9000

# Credenciales (considera usar variables de entorno para producción)
# El token se puede configurar aquí o mediante la variable de entorno SONAR_TOKEN
sonar.login=sqa_6e062ccc7c0d7b869af235eaa89602e8ba4665f9

# Configuración de fuentes
sonar.sources=src
sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/node_modules/**,**/.next/**

# Configuración de pruebas
sonar.tests=src
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Configuración de la rama (requiere Developer Edition o superior)
# sonar.branch.name=Migracion2

# Codificación del código fuente
sonar.sourceEncoding=UTF-8

# Configuración específica para TypeScript
sonar.typescript.tsconfigPath=tsconfig.json

# Análisis de rama (descomenta si usas análisis de rama)
# sonar.branch.name=main
# sonar.branch.target=main

# Configuración de cualidad
sonar.qualitygate.wait=true
